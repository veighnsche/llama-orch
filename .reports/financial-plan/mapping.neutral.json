{
  "version": "v1",
  "placeholder_format": "{{path}}",
  "notes": "Neutral mapping: placeholders zijn dot-paths uit dataset.v1.json. Indien een andere TEMPLATE_KEYS gewenst is, lever die dan aan en we genereren een 1:1 map.",
  "examples": [
    { "path": "company.name", "placeholder": "{{company.name}}" },
    { "path": "plan.start_date", "placeholder": "{{plan.start_date}}" },
    { "path": "offerings[1].pricing.baseline_eur_per_unit", "placeholder": "{{offerings[1].pricing.baseline_eur_per_unit}}" },
    { "path": "costs.fixed_opex_monthly.payroll.founder_gross_eur", "placeholder": "{{costs.fixed_opex_monthly.payroll.founder_gross_eur}}" },
    { "path": "financing.qredits_loan_proposal.principal_eur", "placeholder": "{{financing.qredits_loan_proposal.principal_eur}}" }
  ]
}
