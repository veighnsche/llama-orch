//! TEAM-330: Minimal stub binary for testing build.rs
//!
//! This binary does nothing except compile quickly.
//! Used by build_tests.rs to verify cargo build behavior.

fn main() {
    // Intentionally empty - we just need a binary that compiles fast
}
