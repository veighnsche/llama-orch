[package]
name = "job-registry-interface"
version = "0.1.0"
edition = "2021"
authors = ["TEAM-305"]
description = "Interface trait for job registry to break circular dependency"
license = "GPL-3.0-or-later"
publish = false

[dependencies]
tokio = { workspace = true, features = ["sync"] }
serde_json = { workspace = true }
chrono = "0.4"
