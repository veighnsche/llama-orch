//! Actor inference utilities for the narration system.
//!
//! The actual `extract_service_name` function is now in `observability-narration-core`
//! and is called at runtime via `module_path!()` macro.
//!
//! This module is kept for documentation and potential future compile-time inference.
