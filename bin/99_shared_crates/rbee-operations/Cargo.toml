# TEAM-186: Created shared operation types for rbee-keeper ↔ queen-rbee contract

[package]
name = "rbee-operations"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
