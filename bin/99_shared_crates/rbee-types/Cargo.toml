# TEAM-135: Created by TEAM-135 (scaffolding)
# TEAM-022: Original implementation from hive-core (merged 2025-10-19)
# NOTE: Renamed from hive-core to rbee-types

[package]
name = "rbee-types"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[lib]
name = "rbee_types"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
thiserror = "1.0"
chrono = { version = "0.4", features = ["serde"] }

[dev-dependencies]
tempfile = "3.0"
