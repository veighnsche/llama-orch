[package]
name = "worker-models-bdd"
version = "0.0.0"
edition = "2021"
license = "GPL-3.0-or-later"

[features]
default = ["bdd-cucumber"]
bdd-cucumber = []

[dependencies]
anyhow = "1.0"
cucumber = "0.20"
tokio = { version = "1.0", features = ["full"] }
worker-models = { path = ".." }
worker-gguf = { path = "../../worker-gguf" }

[[bin]]
name = "bdd-runner"
path = "src/main.rs"
