# TEAM-135: Created by TEAM-135 (scaffolding)
# TEAM-210: Updated dependencies for Phase 1 Foundation

[package]
name = "queen-rbee-hive-lifecycle"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[lib]
name = "queen_rbee_hive_lifecycle"
path = "src/lib.rs"

[dependencies]
# TEAM-210: Core dependencies
anyhow = "1.0"
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.11", features = ["json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
once_cell = "1.19"

# TEAM-210: Internal dependencies
daemon-lifecycle = { path = "../../99_shared_crates/daemon-lifecycle" }
observability-narration-core = { path = "../../99_shared_crates/narration-core" }
timeout-enforcer = { path = "../../99_shared_crates/timeout-enforcer" }
rbee-config = { path = "../../99_shared_crates/rbee-config" }
queen-rbee-ssh-client = { path = "../ssh-client" }

[dev-dependencies]
tempfile = "3.8"
nix = { version = "0.27", features = ["signal", "process"] }
