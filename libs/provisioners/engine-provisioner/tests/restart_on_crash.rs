#![cfg(test)]

/// TODO(OwnerC-RESTART): Add a supervised run test that simulates a crash and verifies
/// a restart policy brings the server back and health passes again. Out of scope for MVP
/// but tracked here as a placeholder test (ignored).
#[test]
#[ignore]
fn restart_on_crash_placeholder() {
    eprintln!("restart-on-crash test placeholder (not implemented in MVP)");
}
