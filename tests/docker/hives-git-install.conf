# TEAM-260: Test configuration for git clone + cargo build method
# TOML format for declarative hive configuration
# This config tests the git-based installation (currently broken, needs investigation)

[[hive]]
alias = "docker-test-git"
hostname = "localhost"
ssh_port = 2222
ssh_user = "rbee"
hive_port = 9000
auto_start = false

[hive.install_method]
# Git clone + cargo build method (needs investigation - see INVESTIGATION_REPORT_TEAM_260.md)
git = { repo = "https://github.com/veighnsche/llama-orch.git", branch = "main" }
