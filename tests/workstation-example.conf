# TEAM-260: Example config for real SSH workstation deployment
# This demonstrates using custom config files for different scenarios

[[hive]]
alias = "workstation"
hostname = "192.168.178.29"
ssh_port = 22
ssh_user = "vince"
hive_port = 9000
auto_start = false

[hive.install_method]
# Local binary method (fast, reliable)
# Path is relative to where rbee-keeper runs from
local = { path = "target/debug/rbee-hive" }

# Alternative: Git clone method (slower, needs investigation)
# git = { repo = "https://github.com/veighnsche/llama-orch.git", branch = "main" }

# Alternative: Release download (not yet implemented)
# release = { repo = "veighnsche/llama-orch", tag = "v0.1.0" }
