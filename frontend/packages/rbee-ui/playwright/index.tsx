/**
 * Playwright Component Testing Setup
 *
 * Imports globals.css which includes theme-tokens.css with all CSS variables.
 * No inline variable injection needed - they're built into the CSS.
 */
import '../src/tokens/globals.css'
