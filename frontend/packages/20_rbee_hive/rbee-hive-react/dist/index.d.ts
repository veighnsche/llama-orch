import { Model, Worker } from '@rbee/rbee-hive-sdk';
export declare function useModels(): {
    models: Model[];
    loading: boolean;
};
export declare function useWorkers(): {
    workers: Worker[];
    loading: boolean;
};
