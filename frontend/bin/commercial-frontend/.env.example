# Vite environment variables for the commercial frontend
# Copy this file to `.env.local` (or `.env`) and adjust values.
# Note: Only variables prefixed with VITE_ are exposed to the client.

# Canonical site URL (used for SEO canonical/alternate links and JSON-LD)
VITE_SITE_URL="https://orchyra.example.com"

# Public links surfaced across the site
VITE_GITHUB_URL="https://github.com/veighnsche/llama-orch"
VITE_DOCS_URL="https://docs.orchyra.example.com"
VITE_API_REF_URL="https://docs.orchyra.example.com/api"

# Contact & social (Contact & Legal page)
VITE_CONTACT_EMAIL="info@orchyra.example.com"
VITE_LINKEDIN_URL="https://www.linkedin.com/in/your-handle"

# Hints
# - For local dev, create `.env.local` next to this file; it is gitignored.
# - `VITE_DOCS_URL` is used for the Dev Utility Bar (homepage) "Docs" link; if not set,
#   it falls back to `${VITE_GITHUB_URL}#readme` when GitHub is configured.
# - `VITE_API_REF_URL` controls the optional "API Reference" link in the Dev Utility Bar.
